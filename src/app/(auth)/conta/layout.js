import "./conta.module.css";

export const metadata = {
  title: "Bem-vindo! | Trajetto Express",
  description: "Faça seu cadastro Trajetto Express.",
};

export default function ContaLayout({ children }) {
  return (
    <div>
      {children}
    </div>

  );
}

  