import "./logout.module.css";

export const metadata = {
  title: "Logout | Trajetto Express",
  description: "Volte em breve à sua conta Trajetto Express.",
};

export default function LogoutLayout({ children }) {
  return (
    <div>
      {children}
    </div>

  );
}

  